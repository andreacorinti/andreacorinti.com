---
layout: base.njk
---

{# Questo tag è fondamentale per Bridgy Fed. Assicurati che sia nel layout del singolo post. #}
<a href="https://fed.brid.gy/" hidden></a>

{# Qui dovrai inserire i link per l'endpoint Webmention, solitamente nel <head> del tuo base.njk #}
{# Esempio per webmention.io. Sostituisci "yourdomain.com" con il tuo dominio reale. #}
{# <link rel="webmention" href="https://webmention.io/yourdomain.com/webmention" /> #}
{# <link rel="pingback" href="https://webmention.io/yourdomain.com/xmlrpc" /> #}

{% include 'sitenav.njk' %}

<header class="tdbc-hero">
  <div class="tdbc-container">
    <h1 class="tdbc-ink--primary tdbc-mb-none p-name">{{ title }}</h1> {# p-name per il titolo del post #}
    <time class="dt-published" datetime="{{ date | htmlDateString }}">{{ date | dateDisplay }}</time> {# dt-published per la data #}
    <img src="{{ immagine }}" alt="{{ title }}" title="{{ title }}" itemprop="image">
    <h2 class="tdbc-lead tdbc-content-maxlength tdbc-ink--gray tdbc-mt-md tdbc-mx-auto">
      {# p-summary per il sommario del post #}
      <span class="p-summary">{{ subtitle }}</span>
    </h2>
  </div>
</header>

{# Questo è il contenitore principale del tuo post e dovrebbe avere la classe h-entry #}
<main class="tdbc-container h-entry">
  {# L'autore dovrebbe essere un h-card e apparire una sola volta #}
  <p>Published by <a class="p-author h-card" href="/">Andrea Corinti</a></p>

  {# u-url è l'URL permanente del post. È cruciale per Bridgy Fed. #}
  <a class="u-url" href="{{ permalink }}" hidden></a>

  {# Il contenuto principale del post, con la classe e-content #}
  <article class="tdbc-mx-auto e-content">
    {{ content | safe }}
  </article>

  <section>
    {# La sezione commenti (Utterances) può rimanere, ma non è direttamente collegata a Bridgy Fed/Webmention #}
    </section>
</main>