- Sistemare l'avatar nella home e nel curriculum
- Valutare "post correlati" in fondo ai post: con solo 6 tag ampi il match automatico sarebbe più "altri post della categoria" che correlazione vera — o etichettarlo come tale, o aggiungere un campo frontmatter manuale per i post dove conta di più
- `npm run lint` è rotto da tempo, indipendentemente dall'aggiornamento a Eleventy 3: mancava la dipendenza `stylelint-config-prettier` (mai installata) e, comunque, `.stylelintrc.yaml` usa parecchie regole "stilistiche" che stylelint ha rimosso dal core dalla v15 in poi, più alcuni file `.scss` mai passati dal linter con violazioni BEM pregresse. Non blocca la build/deploy (non è nella pipeline Netlify), ma richiede una revisione a parte della configurazione se si vuole farlo funzionare di nuovo
- `social.andreacorinti.com` (vecchia istanza GoToSocial, pensionata) risponde ancora ma con 404 — il sito ora referenzia solo Misskey, ma quel sottodominio non è servito da questo repo/Netlify, quindi va sistemato a livello di DNS/hosting (spegnerlo del tutto o reindirizzarlo con un 301 verso `/about/`) per far sì che i motori di ricerca smettano di proporlo
- Audit delle dipendenze in `package.json`: `axios`, `lodash`, `typescript`, `nth-check`, `postcss-selector-parser`, `@types/markdown-it` (in `dependencies`) e `postcss-cli` (in `devDependencies`, sostituito da `scripts/minify-css.js`) non risultano usati da nessun file del progetto — da verificare e rimuovere con un giro di `npm uninstall` + rebuild completa
- `src/img/` contiene diverse immagini orfane mai referenziate nei post (es. `avatar2.jpg`, `baseavatar.jpg`, `test-avatar.png`, `test-avatar.webp`) — utile un giro di pulizia, ma da fare con calma: con centinaia di post storici serve un controllo puntuale prima di cancellare
- Molti link `target="_blank"` nei post più vecchi non hanno `rel="noopener"` (piccolo rischio di reverse tabnabbing). Sistemato nei template condivisi (`social.njk`, `about.md`) in questo giro, ma resta da fare un passaggio sui contenuti storici in `src/posts/`
- Nessun header di sicurezza configurato in `netlify.toml` (CSP, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy` sono assenti) — da valutare l'aggiunta di un blocco `[[headers]]`
- Varie ed eventuali
